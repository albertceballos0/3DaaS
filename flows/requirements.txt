# Prefect orchestration (Prefect 3.x — Prefect Cloud)
prefect>=3.0

# GCP SDKs
google-cloud-aiplatform>=1.55.0
google-cloud-storage>=2.16.0
google-cloud-firestore>=2.16.0

# Environment
python-dotenv>=1.0.0
requests>=2.31.0
